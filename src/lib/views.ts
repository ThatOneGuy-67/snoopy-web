/**
 * Central definition of the top-level views that appear in TopMenu / Index.
 * Previously exported from SideNav; extracted here so we can drop the
 * unused SideNav component without breaking imports.
 */
export type SidebarView = 'home' | 'apps' | 'bookmarks' | 'history' | 'downloads';
