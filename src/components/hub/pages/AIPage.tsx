import { Sparkles } from 'lucide-react';
import ComingSoon from '../ComingSoon';

const AIPage = () => (
  <ComingSoon
    title="AI"
    icon={Sparkles}
    description="Chat with an AI assistant right inside Snoopy's Web — coming soon."
  />
);

export default AIPage;
