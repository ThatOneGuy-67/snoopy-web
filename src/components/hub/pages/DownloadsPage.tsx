import { Download } from 'lucide-react';
import ComingSoon from '../ComingSoon';

const DownloadsPage = () => (
  <ComingSoon
    title="Downloads"
    icon={Download}
    description="A dedicated downloads manager is on the way."
  />
);

export default DownloadsPage;
