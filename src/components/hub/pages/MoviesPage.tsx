import { Film } from 'lucide-react';
import ComingSoon from '../ComingSoon';

const MoviesPage = () => (
  <ComingSoon
    title="Movies"
    icon={Film}
    description="Stream your favorite films — this hub is under construction."
  />
);

export default MoviesPage;
